# =============================================================================
# Variables d'environnement - EXEMPLE
# =============================================================================
# Renommez ce fichier en .env et remplissez avec vos vraies valeurs
# NE JAMAIS commiter le fichier .env dans Git !

# Clé API Tiingo (https://www.tiingo.com/)
TIINGO_API_KEY=votre_cle_api_tiingo

# Configuration Email avec Resend (https://resend.com)
RESEND_API_KEY=re_xxxxxxxxxxxx
EMAIL_FROM=onboarding@resend.dev
EMAIL_TO=votre@email.com

# Clé secrète Flask (générez-en une avec: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=votre_cle_secrete_generee

# Code PIN admin pour accéder aux fonctions d'édition (chiffres uniquement)
# Si non défini, le site est en accès complet (mode développement)
ADMIN_PASSWORD=123456

# Base de données (laissez vide pour SQLite local, ou utilisez PostgreSQL)
# DATABASE_URL=postgresql://user:password@host:port/database

# Environnement (development ou production)
FLASK_ENV=development

# Résumé des news (briefings) via API LLM compatible OpenAI
# Sans LLM_API_KEY : repli automatique sur un résumé heuristique (titres groupés).
# Exemples : OpenAI (défaut) | DeepSeek (base_url https://api.deepseek.com, model deepseek-chat)
#            Gemini (base_url https://generativelanguage.googleapis.com/v1beta/openai, model gemini-2.0-flash)
LLM_API_KEY=sk-xxxxxxxxxxxx
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini

